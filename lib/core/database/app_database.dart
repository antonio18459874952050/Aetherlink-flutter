/// Placeholder for the Drift database instance.
///
/// No tables are defined yet. The schema (topic → message → message_block, see
/// `docs/ARCHITECTURE.md` §5 and `docs/DOMAIN_MODEL.md`) and the Drift
/// `@DriftDatabase` instance land in milestone M1. This library exists only to
/// fix the database module's location per `docs/PROJECT_STRUCTURE.md`.
library;
